Fs = 200; % Частота дискретизации (Гц)
fc = 60;  % Частота среза (Гц)
N = 4;     % Порядок фильтра
[z, p, k] = besself(N, 2*pi*fc); % Расчет аналогового прототипа
[num, den] = zp2tf(z, p, k);    % Переход к полиномиальной форме
[numd, dend] = bilinear(num, den, Fs); % Дискретизация (преобразование Tustin)
